{
  "name": "step-cascade",
  "version": "1.0.5",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "prepack": "rm *.js && npm run build && npm test"
  },
  "dependencies": {
    "husky": "^8.0.3",
    "jest": "^29.6.3",
    "ts-jest": "^29.1.1",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "prettier": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
